// Interface for a Role Data Transfer Object (DTO)
export interface RoleDto{
    role_id: number; // Unique identifier for the role
    role_name: string; // Name of the role (e.g., "Admin", "User", "Recruiter")
}
